import 'package:flutter/material.dart';

class CreateDocumentScreen extends StatelessWidget {
  const CreateDocumentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Create New Document");
  }
}
